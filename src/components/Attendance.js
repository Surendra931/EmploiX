import React from 'react';
import { Typography } from '@mui/material';

const Attendance = () => {
  return (
    <Typography variant="h6">Attendance Activity Component</Typography>
  );
};

export default Attendance;
